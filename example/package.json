{
  "name": "http-knocking-example",
  "version": "1.0.0",
  "description": "Example of HTTP Knocking",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "start": "npm run build && node dist/simple-http-server.js"
  },
  "author": "Ryo Ota <nwtgck@gmail.com> (https://github.com/nwtgck)",
  "dependencies": {
    "http-knocking": "file:.."
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
